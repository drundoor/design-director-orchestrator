interface:
  display_name: "Design Director"
  short_description: "Orchestrate UI design, references, and QA"
  default_prompt: "Use $design-director to classify the design intent/platform, define the brief, choose references, discover active states, and set the QA gates."
