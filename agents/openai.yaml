interface:
  display_name: "Design Director"
  short_description: "Route multi-skill design work"
  default_prompt: "Use $design-director to route this UI work, define the design brief, choose references, and set the QA gates."
